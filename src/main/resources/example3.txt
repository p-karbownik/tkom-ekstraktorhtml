resource
{
	tag = [div];

	conditions
	{
		if (child has tag == [a] and has attribute == [href]) or (child.child has tag == [img] and has attribute == [src]);
	}
	export to class = [Link];
	set fields
	{
		field[link_to_image] = from tag[a][1].tag[img][1].attribute[src].text;
		field[description] = from tag[a][1].tag[img][1].attribute[alt].text;
	}
	amount = every;
}
